<Window x:Class="Kechiki.Admin.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Kechiki.Admin.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="Kechiki Admin Login" Height="350" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Kechiki Admin" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            
            <TextBlock Text="Email"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Padding="5"/>
            
            <TextBlock Text="Password"/>
            <PasswordBox x:Name="PasswordBox" Padding="5" PasswordChanged="PasswordBox_PasswordChanged"/>
            
            <CheckBox Content="Remember Me" IsChecked="{Binding RememberMe}" Margin="0,10,0,0"/>
            
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" Margin="0,10" 
                       Visibility="{Binding ErrorMessage, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            
            <Button Command="{Binding LoginCommand}" Content="Login" Padding="10" Margin="0,10,0,0"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"/>
            
            <ProgressBar IsIndeterminate="True" Height="4" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
        </StackPanel>
    </Grid>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <!-- Basic inverse converter would go here or be implemented in code behind/resources -->
    </Window.Resources>
</Window>
