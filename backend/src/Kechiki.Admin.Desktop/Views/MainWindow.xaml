<Window x:Class="Kechiki.Admin.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Kechiki.Admin.Desktop.ViewModels"
        mc:Ignorable="d"
        Title="Kechiki Admin" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Background="#F3F4F6" BorderBrush="#E5E7EB" BorderThickness="0,0,1,0">
            <StackPanel Margin="10">
                <TextBlock Text="Kechiki" FontSize="20" FontWeight="Bold" Margin="10,20"/>
                
                <TextBlock Text="MAIN" Foreground="Gray" FontSize="12" Margin="10,20,10,5"/>
                <Button Content="Dashboard" HorizontalContentAlignment="Left" Padding="10,5" Background="Transparent" BorderThickness="0"/>
                <Button Content="Sellers" HorizontalContentAlignment="Left" Padding="10,5" Background="Transparent" BorderThickness="0"/>
                <Button Content="Catalog" HorizontalContentAlignment="Left" Padding="10,5" Background="Transparent" BorderThickness="0"/>
                
                <TextBlock Text="CONFIGURATION" Foreground="Gray" FontSize="12" Margin="10,20,10,5"/>
                <Button Content="Countries &amp; Laws" HorizontalContentAlignment="Left" Padding="10,5" Background="Transparent" BorderThickness="0"/>
                <Button Content="Settings" HorizontalContentAlignment="Left" Padding="10,5" Background="Transparent" BorderThickness="0"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top Bar -->
            <Border BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="20">
                <Grid>
                    <TextBlock Text="Dashboard" VerticalAlignment="Center" FontSize="18" FontWeight="SemiBold"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding CurrentUserDisplay}" VerticalAlignment="Center"/>
                        <Button Content="Logout" Command="{Binding LogoutCommand}" Padding="10,5"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Content Area -->
            <ContentControl Grid.Row="1" Content="{Binding StartMessage}" Padding="20" VerticalContentAlignment="Center" HorizontalContentAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
